// Arrays JS
let numerosArreglos =[];

// modificar los arrays
numerosArreglos[0]=13;
numerosArreglos[1]=21;
numerosArreglos[4]='hola';

// leer los valores
console.log(`Elemento 1 [0] ${numerosArreglos[0]}`)

//declaración de array
let numeroArreglo = [13,21,0,0,62];

// leer los valores del array
console.log(numeroArreglo.length);
for(let i = 0; i < numeroArreglo.length; i++){
    console.log(`Arreglos[${i}] = ${numeroArreglo[i]}`)
}